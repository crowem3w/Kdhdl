<?xml version="1.0" encoding="utf-8"?>
<!--
  Ripple for a row in the Elements sidebar (see item_elements_category.xml). Follows the Material 3
  navigation-drawer item shape: a fully rounded (pill) 56dp-high container whose state layer /
  ripple is clipped to that pill via the mask. No resting fill, so the sheet's surface shows
  through until the row is pressed.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFFFF" />
            <corners android:radius="28dp" />
        </shape>
    </item>
</ripple>
