<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground service that owns the market-data / live-trading pipeline
         lifecycle (see MarketDataForegroundService). "dataSync" is the
         closest stock foreground-service type to "keeps streaming market
         data and polling account state" - there's no bespoke "trading agent"
         type. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Required on Android 13+ for the persistent status notification to
         actually be shown to the user; requested at runtime during
         onboarding (see OnboardingActivity). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Backs the onboarding battery-optimization-exemption request
         (ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS) - without this, OEM
         background killers can stop the foreground service anyway. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".SyncoraApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Test">

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".onboarding.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.Test.Onboarding" />

        <!-- On-device verification screen for the CPCV/PBO gate and ExperienceLogStore
             kill/restart durability (goals 2 and 3). Its own LAUNCHER entry so it's reachable
             straight from the home screen/app drawer without needing a computer or touching
             MainActivity's UI - see DiagnosticsActivity's kdoc. -->
        <activity
            android:name=".diagnostics.DiagnosticsActivity"
            android:exported="true"
            android:label="Syncora Verify"
            android:theme="@style/Theme.Test">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Owns the market-data/live-trading pipeline lifecycle instead of
             MainActivity - see MarketDataForegroundService's kdoc. Not
             exported: only this app starts/stops it. -->
        <service
            android:name=".service.MarketDataForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>

</manifest>
